# $distribution_name

$description

## Install

```bash
pip install $distribution_name
```

## Usage

Once installed, MyAgent will discover this plugin automatically through the
`myagent.plugins` entry point.
