[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "justagent-go-ship"
version = "0.1.0"
description = "JustAgent-CLI language pack: Go build artifacts and verify hints"
requires-python = ">=3.10"
dependencies = [
    "justagent>=1.0.0",
    "justagent-sdk>=1.0.0",
]

[project.entry-points."justagent.plugins"]
go = "myagent_go_ship.plugin:register"
