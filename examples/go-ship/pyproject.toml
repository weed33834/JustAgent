[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "myagent-go-ship"
version = "0.1.0"
description = "MyAgent-CLI language pack: Go build artifacts and verify hints"
requires-python = ">=3.10"
dependencies = [
    "myagent>=1.0.0",
    "myagent-sdk>=1.0.0",
]

[project.entry-points."myagent.plugins"]
go = "myagent_go_ship.plugin:register"
