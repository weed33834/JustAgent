[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "myagent-java-ship"
version = "0.1.0"
description = "MyAgent-CLI language pack: Java build artifacts and verify hints"
requires-python = ">=3.10"
dependencies = [
    "myagent>=1.0.0",
    "myagent-sdk>=1.0.0",
]

[project.entry-points."myagent.plugins"]
java = "myagent_java_ship.plugin:register"
