[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/justagent_sdk"]
include = ["src/justagent_sdk/templates/**/*"]

[project]
name = "justagent-sdk"
version = "3.0.0"
description = "JustAgent plugin development SDK"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.11"
authors = [
    { name = "JustAgent Team", email = "team@justagent.dev" },
]
dependencies = [
    "justagent>=3.0.0",
]

[project.urls]
Repository = "https://gitcode.com/badhope/JustAgent"
Issues = "https://gitcode.com/badhope/JustAgent/issues"

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP", "B", "C4", "SIM"]
ignore = ["E501", "B008", "TCH"]

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.pyright]
pythonVersion = "3.11"
typeCheckingMode = "strict"
include = ["src"]
exclude = ["**/__pycache__", "**/tests"]
reportMissingTypeStubs = false

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--cov=justagent_sdk --cov-report=term-missing --cov-fail-under=85"

[tool.coverage.run]
source = ["src/justagent_sdk"]
branch = true

[dependency-groups]
dev = [
    "ruff>=0.4.0",
    "pyright>=1.1.360",
    "pytest>=8.2.0",
    "pytest-cov>=5.0.0",
    "pytest-mock>=3.14.0",
]

[tool.uv.sources]
justagent = { workspace = true }
